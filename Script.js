// Part 1:
const PI= 3.1415;
const radius= 5;
constant area= PI*radius*radius;

const minimum space per plant= 0.8
const number of plants=20;

const capacity80=0.8*max capacity;
const capcity50=0.5*max capacity;


if (number of plants > capacity80) {
    console.log (Capacity reached);
} else if (number of plants>capacity50) {
    console.log(Steady Growth);
} else {
    console.log(Grow more plants);
}
